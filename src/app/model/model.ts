export interface Employee {
    id: number,
    customerId: string,
    name: string,
    description: string,
    street: string,
    postal_code: string,
    city: string,
    startDate: string,
    endDate: string
}